# Enterprise AI Workbench Foundation Package

Copy the included folders into the root of:

`C:\Users\Josh.Corbit\vite-project`

Then follow:

`backend/main_update_instructions.txt`

This package does not replace `main.py` and does not remove existing code.
